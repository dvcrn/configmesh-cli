# Configmesh CLI

Configmesh CLI syncs selected configuration files between devices using an
encrypted, pluggable backend. This repository is the distribution channel for
prebuilt binaries and the npm wrapper.

## Install

```
npm install -g configmesh
```

This downloads a prebuilt binary at install time.
